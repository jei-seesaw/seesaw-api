services:
  mariadb:
    image: mariadb:11
    ports:
      - "3307:3306"
    environment:
      MARIADB_DATABASE: seesaw
      MARIADB_USER: seesaw
      MARIADB_PASSWORD: seesaw
      MARIADB_ROOT_PASSWORD: seesaw_root
    volumes:
      - mariadb-data:/var/lib/mysql

volumes:
  mariadb-data:
